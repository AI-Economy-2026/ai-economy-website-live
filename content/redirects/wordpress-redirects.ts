// WordPress to Next.js URL redirect mapping
// Add entries here when WordPress URLs are provided
export const wordpressRedirects: Array<{
  source: string;
  destination: string;
  permanent: boolean;
}> = [
  // Example structure:
  // { source: '/old-wordpress-page', destination: '/new-page', permanent: true },
];
